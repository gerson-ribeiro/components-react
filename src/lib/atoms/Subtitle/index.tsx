import { GenericItem } from "../../models/generic-item";
import { Title as TitleStyled } from "./styles";

const Subtitle: React.FC<GenericItem> = (props) => <TitleStyled {...props} />;

export default Subtitle;
